# frozen_string_literal: true

source 'https://rubygems.org'

# Runtime dependencies
gem 'json', '~> 2.6'

# Ethereum interaction (required for public backends)
gem 'eth', '~> 0.5'

group :development, :test do
  gem 'rake', '~> 13.0'
  gem 'rspec', '~> 3.12'
  gem 'rubocop', '~> 1.50'
  gem 'simplecov', '~> 0.22', require: false
end
